version: '3.8'

services:
  # Backend Service with 2 replicas for load balancing
  backend-service:
    build: ./backend
    deploy:
      replicas: 2
    networks:
      - app-network

  # NGINX Reverse Proxy
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - backend-service
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
